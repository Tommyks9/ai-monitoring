# Optional: point a local Mac dashboard to a remote agent runtime.
# Leave empty to use bundled mock monitoring data.
AGENT_RUNTIME_URL=http://localhost:4000

# Optional bearer token for private agent runtime APIs.
# This is read server-side by Next.js and must not be prefixed with NEXT_PUBLIC_.
AGENT_RUNTIME_TOKEN=crsr_36b82af8de319320cface68c0270134151490a3e184c42f630143ce3130e3fb7
